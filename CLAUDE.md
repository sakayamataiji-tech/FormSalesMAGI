# FormSalesMAGI ── フォーム営業文面 自動生成「マギシステム」

## 全体像

エヴァンゲリオンの MAGI と同じく、性格の違う3体が独立に案を出し、裁判官が統合して結論を出す仕組みです。
対象は「問い合わせフォーム経由の営業文面（フォーム営業）」。送り手は §自社プロフィール（カリトルくん）。

```
input/companies.csv（1行 = 1社）
        │
        ├──► opener  （開封率に全振り）   ─┐
        ├──► replier （返信率に全振り）   ─┼─ 並列で起草（各1案）
        └──► closer  （契約率に全振り）   ─┘
                                            │
                                            ▼
                                   judge（裁判官）
                          採点 → 合成 or 選抜 → 採用 / 差し戻し
                                            │
                     差し戻し ──► 指名された1体だけ再起草（最大3回）
                                            │
                                            ▼
                              output/results.csv に追記
```

| 役割 | ファイル | 全振りする指標 |
|------|----------|----------------|
| 共通ルール | `.claude/skills/karitoru-form/SKILL.md` | 絶対条件・品質条件・自社プロフィール・出力形式 |
| 起草者① opener | `.claude/agents/opener.md` | 開封率（件名・冒頭・宛名） |
| 起草者② replier | `.claude/agents/replier.md` | 返信率（パーソナライズ・なぜ今・アクション1つ） |
| 起草者③ closer | `.claude/agents/closer.md` | 契約率（USP・FAB+E・実績） |
| 裁判官 judge | `.claude/agents/judge.md` | 採点・合成・差し戻し |

### 絶対条件（全エージェント共通・違反は失格）

1. 本文 400字以内（改行・空白除く）
2. 件名に【】と数字
3. 文頭は「〇〇部 〇〇様」

品質条件（パーソナライズ／自社明示＋実績／なぜ今／アクション1つ／FAB+E／USP一言）は SKILL.md 参照。

## フォルダ構成

```
FormSalesMAGI/
├── CLAUDE.md                          ← このファイル（親セッションの動かし方）
├── .claude/
│   ├── skills/karitoru-form/SKILL.md  ← 共通ルール
│   └── agents/
│       ├── opener.md
│       ├── replier.md
│       ├── closer.md
│       └── judge.md
├── scripts/
│   ├── build_companies.py             ← 営業リスト → input/companies.csv 変換
│   └── append_result.py               ← 最終文面を検証して output/results.csv に追記
├── input/
│   ├── target_list_raw.csv            ← 元の営業リスト（企業名/業界/規模/HP/フォームURL/送信可否）
│   ├── companies.csv                  ← 会社名,部署,担当者,事業内容,課題仮説(+HP,フォームURL)
│   └── sample_companies.csv           ← 動作確認用サンプル3社
└── output/
    └── results.csv                    ← 実行時に生成・追記
```

## 現在の設定（2026-09 時点）

- **送信元（自社）: 株式会社Lei Hau'oli／商材: SES（フロントエンド特化の開発支援）**。プロフィールは SKILL.md §4。
- **ターゲット: `input/target_list_raw.csv` の 2,115社**（Webアプリ・SaaS・ゲーム・医療機器・セキュリティ）。`scripts/build_companies.py` で「フォームあり & 営業禁止なし」の 1,406社を `companies.csv` に変換済み。
- 元リストに部署名・担当者名が無いため、部署は業界から推定（開発部／技術部／情報システム部）、担当者は「ご担当者」（→「開発部 ご担当者様」）。事業内容・課題仮説は業界×規模のテンプレートから生成しているので、**HPを読める環境では起草前に事業内容・課題仮説を個社情報で上書きすると精度が上がる**。
- パイロット3社（セーフィー／グラスホッパー・マニファクチュア／パロニム）は results.csv に記録済み。

## 親セッション（あなた）の動かし方

ユーザーから「実行して」「companies.csv を回して」「〇〇社の文面を作って」と言われたら、以下の手順で動く。

### 0. 準備

- `input/companies.csv` を読む。ヘッダーは `会社名,部署,担当者,事業内容,課題仮説`。
- `output/results.csv` が無ければヘッダー行だけ作る（列は §出力仕様）。
- 対象行が指定されていれば、その行だけ処理する。指定が無ければ全行を上から順に処理する。

### 1. 1社ぶんの入力を組み立てる

各起草者に渡すプロンプトは共通で、次の形にする（余計な指示は足さない。ルールは SKILL.md と各エージェント定義に書いてある）。

```
以下の1社に対するフォーム営業文面を、あなたの役割に従って1案だけ書いてください。

会社名: {会社名}
部署: {部署}
担当者: {担当者}
事業内容: {事業内容}
課題仮説: {課題仮説}
```

### 2. 3体を並列で起草させる

`Agent` ツールを **同じメッセージ内で3回** 呼び、並列に走らせる。

- `subagent_type: "opener"`
- `subagent_type: "replier"`
- `subagent_type: "closer"`

3案が揃うまで次に進まない。いずれかが出力フォーマット（SKILL.md §5）を守っていなければ、その1体だけ「§5のフォーマットで1案だけ出力し直してください」と再実行する。

### 3. judge で統合する

`Agent` ツールで `subagent_type: "judge"` を呼ぶ。渡すプロンプトは次の形。

```
以下の会社に対する3案を採点し、合成または選抜で最終文面を確定してください。

【会社データ】
会社名: ... / 部署: ... / 担当者: ... / 事業内容: ... / 課題仮説: ...

【opener案】
（openerの出力全文）

【replier案】
（replierの出力全文）

【closer案】
（closerの出力全文）
```

judge の出力末尾 `## VERDICT` ブロックを読む。

- `STATUS: ADOPT` → 手順5へ。
- `STATUS: REMAND` → 手順4へ。

### 4. 差し戻し（最大3回）

- `TARGET:` に書かれた1体だけを再実行する。プロンプトは手順1の共通形の末尾に次を追加する。

```
【裁判官からの差し戻し指示】
{INSTRUCTION の内容}

【あなたの前回案】
{その起草者の前回出力}
```

- 他の2体の案はそのまま使い、新しい案と合わせて手順3（judge）をやり直す。
- 差し戻し回数を数える。**3回差し戻しても ADOPT にならなければ**、その時点で judge が最も高く採点した案（失格でないもの）を選抜扱いで採用し、結果の `備考` 列に `差し戻し上限到達` と書く。失格しか残っていなければ `備考` に `失格のみ・要人手` と書き、件名・本文は空で記録する。

### 5. 最終文面の検証と記録

judge を信用しつつ、書き込み前に親セッション側でも最低限の機械チェックを行う（Bash が使えれば）。

```bash
python3 - <<'PY'
import re
body = """（最終本文）"""; subject = """（最終件名）"""
n = len(re.sub(r"\s", "", body)); assert n <= 400, f"A1 NG: {n}"
assert "【" in subject and "】" in subject and re.search(r"[0-9０-９]", subject), "A2 NG"
assert re.match(r"^\S+\s+\S+様$", body.strip().splitlines()[0]), "A3 NG"
print("OK", n)
PY
```

NG なら judge に「最終案が絶対条件 {A?} に違反しています。修正して VERDICT を出し直してください」と1回だけ再実行する。それでも NG なら `備考` に `絶対条件NG` と書いて記録し、人に報告する。

OK なら `output/results.csv` に1行追記する。追記は `scripts/append_result.py` に任せる（JSON を渡すと A1〜A3 を再検証し、CSV エスケープも処理する）。

```bash
python3 scripts/append_result.py /path/to/result.json
```

### 6. 次の行へ

全行が終わったら、処理した社数・ADOPT数・差し戻し回数の合計・要人手の社数をユーザーに報告する。

## 出力仕様 `output/results.csv`

UTF-8（BOMなし）、ヘッダーあり。本文は改行を含むので **必ずダブルクォートで囲み、内部の `"` は `""` に置換** する（Python の `csv` モジュールに任せるのが安全）。

| 列 | 内容 |
|----|------|
| 処理日時 | ISO 8601（例 2026-09-09T10:00:00+09:00） |
| 会社名 | 入力そのまま |
| 部署 | 入力そのまま |
| 担当者 | 入力そのまま |
| 件名 | 最終文面の件名 |
| 本文 | 最終文面の本文（宛名〜署名） |
| 本文字数 | 改行・空白除く |
| opener_score | judge の採点（0〜100） |
| replier_score | 同上 |
| closer_score | 同上 |
| final_score | 最終案の総合点 |
| モード | 合成 / 選抜 |
| 差し戻し回数 | 0〜3 |
| 採用理由 | judge「採用理由」の要約（200字以内、改行なし） |
| 備考 | 空 / 差し戻し上限到達 / 失格のみ・要人手 / 絶対条件NG |

## 運用メモ

- **自社プロフィール（SKILL.md §4）はサンプル値。** 本番前に必ず実値へ差し替えること。起草者はここにない数字を創作できないので、ここが空だと文面も薄くなる。
- 起草者に「もっと短く」「もっと攻めて」などの追加指示を親が足さない。調整は SKILL.md か各エージェント定義を編集して行う（全社に一貫して効かせるため）。
- 1社あたりの目安: 起草3体並列 → judge 1回 → 差し戻し0〜1回。差し戻しが毎回3回に達するなら、課題仮説の質か自社プロフィールの不足を疑う。
- `input/companies.csv` の「課題仮説」が空の行は、事業内容から親セッションが1行で仮説を補ってから起草者に渡す（補った旨を `備考` に書く）。
